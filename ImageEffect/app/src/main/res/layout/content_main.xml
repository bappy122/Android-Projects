<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/activity_main"
    tools:context="com.example.ak_bappy.imageeffect.MainActivity">

    <ImageView
        android:layout_width="370dp"
        android:layout_height="415dp"
        android:id="@+id/imageView"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:text="Invert"
        android:background="@drawable/trnsparent"
        android:id="@+id/button"
        android:textSize="20dp"
        android:textColor="#ffffff"
        android:onClick="onClickInvert"
        android:layout_alignParentBottom="true"
        android:layout_alignLeft="@+id/captureButton"
        android:layout_alignStart="@+id/captureButton" />

    <Button
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:text="CApture"
        android:background="@drawable/trnsparent"
        android:id="@+id/captureButton"
        android:textSize="20dp"
        android:textColor="#ffffff"
        android:onClick="startCamera"
        android:layout_below="@+id/imageView"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
